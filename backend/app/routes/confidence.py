# Confidence Score is served via the drivers router at GET /drivers/{id}/confidence
# This module is a placeholder for future standalone scoring endpoints
# (e.g., batch scoring for cooperative dashboard)

from fastapi import APIRouter

router = APIRouter()
